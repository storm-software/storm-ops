---
name: Weekly codeowners cleanup
on:
  workflow_dispatch:
  schedule:
    - cron: "3 2 1 * *"

permissions:
  contents: read

jobs:
  clean-owners:
    name: clean-owners
    runs-on: ubuntu-latest
    permissions:
      issues: write

    steps:
      - name: Run clean-owners action
        uses: github-community-projects/cleanowners@45f292ce59392482f81f03e5ef3c27daaf06183e # v2.1.13
        env:
          GH_TOKEN: ${{ secrets.STORM_BOT_GITHUB_TOKEN }}
          ORGANIZATION: storm-software
