{
  "name": "conventional-changelog",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/conventional-changelog/src",
  "targets": {
    "build-base": {
      "cache": true,
      "inputs": ["default", "^production"],
      "outputs": ["{projectRoot}/dist"],
      "executor": "nx:run-commands",
      "dependsOn": ["clean", "^build"],
      "options": {
        "command": "tsup --config=tsup.config.ts",
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "cache": true,
      "inputs": ["default", "^production"],
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "executor": "nx:run-commands",
      "dependsOn": ["build-base"],
      "options": {
        "commands": [
          "pnpm copyfiles LICENSE dist/packages/conventional-changelog",
          "pnpm copyfiles --up=2 ./packages/conventional-changelog/*.md ./packages/conventional-changelog/package.json dist/packages/conventional-changelog",
          "pnpm copyfiles --up=3 \"./packages/conventional-changelog/dist/**/*\" dist/packages/conventional-changelog/dist",
          "pnpm copyfiles --up=3 \"./packages/conventional-changelog/src/templates/**/*\" dist/packages/conventional-changelog/templates"
        ],
        "parallel": false,
        "color": true
      }
    },
    "nx-release-publish": {
      "executor": "@storm-software/workspace-tools:npm-publish"
    }
  },
  "tags": ["platform:node"],
  "implicitDependencies": ["config-tools"]
}
