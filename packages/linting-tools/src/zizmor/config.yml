rules:
  template-injection:
    ignore:
      - ci-reusable.yml:80
      - ci-reusable.yml:86
      - ci.yml
  dangerous-triggers:
    ignore:
      - greetings.yml:3:1
  use-trusted-publishing:
    ignore:
      # ignore line 12, column 10 on pypi.yml
      # - pypi.yml:12:10
