{
  "name": "eslint-plugin-banner",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/eslint-plugin-banner/src",
  "targets": {
    "build-base": {
      "executor": "nx:run-commands",
      "dependsOn": ["clean", "^build"],
      "options": {
        "command": "tsdown --config=tsdown.config.ts",
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["build-base"],
      "options": {
        "commands": [
          "pnpm copyfiles LICENSE dist/packages/eslint-plugin-banner",
          "pnpm copyfiles --up=2 ./packages/eslint-plugin-banner/*.md ./packages/eslint-plugin-banner/package.json dist/packages/eslint-plugin-banner",
          "pnpm copyfiles --up=3 \"./packages/eslint-plugin-banner/dist/**/*\" dist/packages/eslint-plugin-banner/dist",
          "pnpm copyfiles --up=3 \"./packages/eslint-plugin-banner/docs/**/*\" dist/packages/eslint-plugin-banner/docs"
        ]
      }
    },
    "docs": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/docs"],
      "dependsOn": ["build"],
      "options": {
        "cwd": "{projectRoot}",
        "command": "pnpm exec eslint-doc-generator"
      }
    },
    "nx-release-publish": {
      "executor": "@storm-software/workspace-tools:npm-publish"
    }
  },
  "tags": ["platform:node"]
}
